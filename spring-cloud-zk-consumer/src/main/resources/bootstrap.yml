spring:
  application:
    name: zk-consumer
  cloud:
    zookeeper:
      connect-string: localhost:2181
      enabled: true
      discovery:
        register: true
        root: /server # 在zookeeper中的注册目录